[project]
name = "db"
version = "0.1.0"
description = "Shared database package"
requires-python = ">=3.12"

dependencies = [
    "psycopg2-binary>=2.9.11",
    "pydantic>=2.12.5",
    "sqlalchemy>=2.0.49",
]
